import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  grid: {
    "margin-top": "40px",
    "margin-bottom": "40px",
    background: "#f3f3f3",
    padding: "20px",
    "border-radius": "10px",
  },
  button: {
    width: "100%",
    "margin-top": "15px",
  },
});

export default useStyles;
